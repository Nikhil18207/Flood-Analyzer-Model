import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Flood Analyzer App - AI-Powered Flood Risk Assessment",
  description: "Comprehensive flood risk assessment application using Next.js, FastAPI, and Google Gemini AI for intelligent terrain analysis and coordinate-based risk evaluation.",
  keywords: ["flood analysis", "risk assessment", "AI", "Gemini", "Next.js", "FastAPI"],
  authors: [{ name: "Flood Analyzer Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}